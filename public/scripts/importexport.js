// check if DOM is loaded
document.addEventListener("DOMContentLoaded", () => {
    console.log("DOM has been loaded and ready!");
    /*
    //update #filename element with uploaded file name
    document.querySelector("#fileUpload").onchange = () => {
        document.querySelector("#filename").innerHTML = this.files[0].name;
    }
    */
});
